# NodeGoat files that genuinely should not produce findings.
# The previous list of vendored old jQuery/Bootstrap was WRONG — those
# have real CVEs and an SCA tool finding them is desired signal, not
# noise. Add entries here only if a tool flags something a human review
# confirms is benign.
